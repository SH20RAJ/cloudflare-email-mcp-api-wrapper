{
  "name": "cf-email-mcp-api",
  "version": "1.0.0",
  "description": "Open-source Cloudflare Worker wrapper exposing email send API + MCP endpoint",
  "main": "test-api.js",
  "scripts": {
    "test:api": "node test-api.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "mimetext": "^3.0.24"
  },
  "devDependencies": {
    "wrangler": "^4.83.0"
  }
}
